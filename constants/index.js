import {Logo, Lock, User, Email, Search, Photo} from './images';
import {primaryColor, white, secondaryColor, gray} from './colors';

export {
  Logo,
  primaryColor,
  white,
  secondaryColor,
  gray,
  Lock,
  User,
  Email,
  Search,
  Photo,
};
